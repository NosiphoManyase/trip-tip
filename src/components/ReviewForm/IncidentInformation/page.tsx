import React from 'react'

const page = () => {

const handleClick = () => {
  // submit details to firebase api
  // redirect to review page
}

  return (
    <div>
        <p>incident details</p>
        <div onClick={() => handleClick}>submit</div>
    </div>
  )
}

export default page